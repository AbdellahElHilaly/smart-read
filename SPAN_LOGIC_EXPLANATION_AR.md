# شرح منطق تقسيم النص إلى Spans 🔍

## لماذا نقسم النص إلى Spans؟

عند بناء تطبيق تفاعلي لممارسة اللغة الإنجليزية، نحتاج إلى أن يكون كل كلمة **قابلة للتفاعل بشكل مستقل**.

إذا تركنا النص كنص عادي، لن نستطيع:
- ❌ معرفة أي كلمة يضغط عليها المستخدم
- ❌ تطبيق أنماط مختلفة على كلمات مختلفة
- ❌ تتبع حالة كل كلمة (مترجمة أم لا)

## الحل: عنصر Span

عنصر `<span>` في HTML هو عنصر **inline** يساعدنا على:
1. **تحديد كل كلمة بشكل فريد**
2. **إضافة معرفات ومعلومات إضافية** باستخدام `data attributes`
3. **تطبيق تأثيرات بصرية** بسهولة
4. **إرفاق أحداث الفأرة** (كلك، hover، إلخ)

## العملية خطوة بخطوة

### الخطوة 1: النص الأصلي
```
"Black holes are among the most fascinating objects in the universe"
```

### الخطوة 2: تقسيم النص إلى كلمات
```javascript
const words = "Black holes are among the most fascinating objects in the universe".split(/\s+/);
// Result: ["Black", "holes", "are", "among", "the", "most", "fascinating", "objects", "in", "the", "universe"]
```

### الخطوة 3: إنشاء Span لكل كلمة
```html
<!-- قبل: نص عادي -->
Black holes are among the most fascinating objects

<!-- بعد: كل كلمة في span -->
<span class="word" data-en="Black" data-ar="أسود">Black</span>
<span class="word" data-en="holes" data-ar="ثقوب">holes</span>
<span class="word" data-en="are" data-ar="هي">are</span>
<!-- ... والباقي -->
```

### الخطوة 4: إضافة أحداث الكلك
```javascript
// عند النقر على أي span
document.addEventListener('click', function(event) {
    if (event.target.classList.contains('word')) {
        const enWord = event.target.getAttribute('data-en');      // "Black"
        const arWord = event.target.getAttribute('data-ar');      // "أسود"
        const currentText = event.target.textContent;             // الكلمة المعروضة حالياً
        
        // تبديل بين الإنجليزية والعربية
        if (currentText === enWord) {
            event.target.textContent = arWord;  // عرض العربية
            event.target.classList.add('translated');  // تطبيق الأنماط
        } else {
            event.target.textContent = enWord;  // عودة للإنجليزية
            event.target.classList.remove('translated');
        }
    }
});
```

## البنية الموصى بها للـ HTML

```html
<p id="textContent">
    <span class="word" data-en="word1" data-ar="الترجمة1">word1</span>
    <span class="word" data-en="word2" data-ar="الترجمة2">word2</span>
    <!-- ... المزيد من الكلمات -->
</p>
```

## الخصائص (`Data Attributes`)

| الخاصية | الوصف | المثال |
|----------|--------|--------|
| `data-en` | كلمة إنجليزية | `data-en="Black"` |
| `data-ar` | الترجمة العربية | `data-ar="أسود"` |
| `class="word"` | معرّف الفئة | للاستهداف بـ CSS و JavaScript |

## الأنماط (CSS) الضروية

```css
.word {
    cursor: pointer;          /* يشير إلى أنها قابلة للنقر */
    display: inline-block;    /* تسمح بتطبيق padding و margin */
    padding: 8px 6px;         /* مساحة حول الكلمة */
    border-radius: 5px;       /* زوايا دائرية */
    transition: all 0.3s ease; /* انتقالات سلسة */
}

.word:hover {
    background-color: rgba(52, 152, 219, 0.15);  /* تأثير عند الإشارة */
    transform: translateY(-2px);                   /* حركة طفيفة لأعلى */
}

.word.translated {
    background-color: #e74c3c;  /* لون أحمر للكلمات المترجمة */
    color: white;                /* نص أبيض للوضوح */
    font-weight: 600;            /* نص أثقل */
}
```

## مثال عملي كامل

### HTML
```html
<p id="textContent">
    <span class="word" data-en="AI" data-ar="الذكاء الاصطناعي">AI</span>
    <span class="word" data-en="is" data-ar="هي">is</span>
    <span class="word" data-en="powerful" data-ar="قوية">powerful</span>
</p>
```

### JavaScript
```javascript
// الحصول على جميع كلمات
const words = document.querySelectorAll('.word');

// إضافة حدث كلك لكل كلمة
words.forEach(word => {
    word.addEventListener('click', function() {
        const en = this.getAttribute('data-en');
        const ar = this.getAttribute('data-ar');
        
        // التبديل
        if (this.textContent === en) {
            this.textContent = ar;
            this.classList.add('translated');
        } else {
            this.textContent = en;
            this.classList.remove('translated');
        }
    });
});
```

### الحالة الأولى (عند التحميل)
```
┌──────────┬──────┬──────────┐
│    AI    │  is  │ powerful │
└──────────┴──────┴──────────┘
```

### بعد النقر على "AI"
```
┌─────────────────────────┬──────┬──────────┐
│ الذكاء الاصطناعي (أحمر) │  is  │ powerful │
└─────────────────────────┴──────┴──────────┘
```

### بعد النقر على "is"
```
┌─────────────────────────┬──────┬──────────┐
│ الذكاء الاصطناعي (أحمر) │ هي   │ powerful │
│                         │(أحمر)│          │
└─────────────────────────┴──────┴──────────┘
```

## الفوائد الرئيسية

✅ **الكفاءة**: كل كلمة يمكن التعامل معها بشكل منفصل

✅ **سهولة الصيانة**: يمكن تحديث البيانات في `data.json` دون تغيير الكود

✅ **الأداء**: استخدام `data attributes` أسرع من البحث في DOM

✅ **المرونة**: يمكن إضافة المزيد من المعلومات (معدل الصعوبة، النطق، إلخ)

✅ **التجربة المستخدم**: تأثيرات بصرية واضحة وفورية

## حالات الاستخدام الأخرى

هذا النمط مفيد أيضاً لـ:
- 📖 تطبيقات قراءة البطاقات (flashcards)
- 🎮 الألعاب التعليمية
- 📝 محررات النصوص المتقدمة
- 🔍 أدوات البحث والاستبدال
- 🌐 تطبيقات تعلم اللغات

---

**الخلاصة**: تقسيم النص إلى spans يعطينا **التحكم الكامل** على مستوى الكلمة الفردية، مما يتيح تطبيقات تفاعلية قوية وسهلة الاستخدام! 🚀